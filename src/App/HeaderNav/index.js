import React from "react";

export default (props) => (
    <nav className="nav" style={{position: "absolute", ...props.style}}>
        <div className="nav-left">
            <section className="nav-item">
                airtoxin
            </section>
        </div>
    </nav>
);
